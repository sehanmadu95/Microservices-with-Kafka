# Getting Started

## How to start
First thing you need to do is run the docker container

run the following command in the root directory of the projects:
````
docker compose -v up -d
````


to stop the containers do

````
docker compose down
````

you might have the delete pre-existing volumes if you are encountering issues